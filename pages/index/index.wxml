<!--index.wxml-->
<view class="container">
  <view class="clock">{{ timeStr }}</view>

  <view
    class="btn {{ isStart ? 'stop' : 'start'}}"
    bindtap='handleClick'
  >
    {{ isStart ? '停止' : '开始' }}
  </view>
<van-dialog id="van-dialog" />
</view>
